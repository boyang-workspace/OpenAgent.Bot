<header class="site-header">
  <div class="header-inner">
    <form class="site-search" action="/search" method="get" role="search" data-analytics="search">
      <label>
        <span>Search</span>
        <input type="search" name="q" placeholder="Search open AI resources" />
      </label>
    </form>
    <nav aria-label="Primary navigation">
      <a class="submit-link" href="/submit" data-analytics="nav" data-analytics-payload='{"label":"Submit","href":"/submit"}'>Submit</a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    background: rgba(14, 19, 18, 0.92);
    backdrop-filter: blur(16px);
    border-bottom: 1px solid var(--dark-line);
    left: 236px;
    position: fixed;
    right: 0;
    top: 0;
    z-index: 12;
  }

  .header-inner {
    align-items: center;
    display: flex;
    gap: 16px;
    min-height: 58px;
    justify-content: space-between;
    margin: 0 auto;
    max-width: 1180px;
    padding: 0 26px;
  }

  .site-search {
    flex: 1 1 520px;
    max-width: 680px;
  }

  .site-search label {
    display: block;
    position: relative;
  }

  .site-search span {
    height: 1px;
    left: -999px;
    overflow: hidden;
    position: absolute;
    top: auto;
    width: 1px;
  }

  .site-search input {
    background: rgba(237, 237, 236, 0.06);
    border: 1px solid var(--dark-line);
    border-radius: 0;
    color: var(--dark-text);
    font: inherit;
    font-size: 0.84rem;
    font-weight: 400;
    min-height: 34px;
    outline: none;
    padding: 0 14px;
    width: 100%;
  }

  .site-search input:focus {
    border-color: var(--dark-line-strong);
    box-shadow: 0 0 0 3px rgba(237, 237, 236, 0.08);
  }

  .site-search input::placeholder {
    color: var(--dark-muted);
    font-weight: 400;
  }

  nav {
    display: flex;
    flex-wrap: wrap;
    gap: 8px 18px;
    justify-content: flex-end;
  }

  nav a {
    color: var(--dark-muted);
    font-size: 0.84rem;
    font-weight: 520;
  }

  nav a:hover {
    color: var(--dark-text);
  }

  .submit-link {
    border: 1px solid var(--dark-line);
    border-radius: 0;
    color: var(--dark-text);
    font-family: var(--mono);
    font-size: 0.72rem;
    font-weight: 800;
    letter-spacing: 0.06em;
    padding: 8px 12px;
    text-transform: uppercase;
  }

  .submit-link:hover {
    background: rgba(237, 237, 236, 0.08);
  }

  @media (max-width: 920px) {
    .site-header {
      left: 0;
    }

    .header-inner {
      align-items: center;
      flex-wrap: nowrap;
      min-height: 58px;
      padding: 10px 18px;
    }

    nav {
      flex: 0 0 auto;
      gap: 10px;
    }

    .site-search {
      flex: 1 1 auto;
      max-width: none;
      min-width: 0;
    }
  }
</style>
