<header class="site-header">
  <div class="header-inner">
    <form class="site-search" action="/search" method="get" role="search" data-analytics="search">
      <label>
        <span>Search</span>
        <input type="search" name="q" placeholder="Search agents, models, skills..." />
      </label>
    </form>
  </div>
</header>

<style>
  .site-header {
    background: rgba(246, 247, 248, 0.9);
    backdrop-filter: blur(16px);
    border-bottom: 1px solid var(--line);
    left: 236px;
    position: fixed;
    right: 0;
    top: 0;
    z-index: 12;
  }

  .header-inner {
    align-items: center;
    display: flex;
    gap: 14px;
    min-height: 58px;
    justify-content: flex-start;
    margin: 0 auto;
    max-width: 1180px;
    padding: 0 26px;
  }

  .site-search {
    flex: 1 1 520px;
    max-width: 680px;
  }

  .site-search label {
    display: block;
    position: relative;
  }

  .site-search span {
    height: 1px;
    left: -999px;
    overflow: hidden;
    position: absolute;
    top: auto;
    width: 1px;
  }

  .site-search input {
    background: var(--surface);
    border: 1px solid var(--line);
    border-radius: 8px;
    color: var(--ink);
    font: inherit;
    font-size: 0.82rem;
    font-weight: 400;
    min-height: 34px;
    outline: none;
    padding: 0 14px;
    width: 100%;
  }

  .site-search input:focus {
    border-color: var(--line-strong);
    box-shadow: 0 0 0 3px rgba(37, 99, 235, 0.08);
  }

  .site-search input::placeholder {
    color: var(--subtle);
    font-weight: 400;
  }

  @media (max-width: 920px) {
    .site-header {
      left: 0;
    }

    .header-inner {
      align-items: center;
      flex-wrap: nowrap;
      min-height: 58px;
      padding: 10px 18px;
    }

    .site-search {
      flex: 1 1 auto;
      max-width: none;
      min-width: 0;
    }
  }
</style>
