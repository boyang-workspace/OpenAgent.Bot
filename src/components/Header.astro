<header class="site-header">
  <div class="shell header-inner">
    <a class="brand" href="/" aria-label="OpenAgent.bot home">
      <span class="brand-mark">OA</span>
      <span>OpenAgent.bot</span>
    </a>
    <nav aria-label="Primary navigation">
      <a href="/blog">Blog</a>
      <a href="/manifesto">Manifesto</a>
      <a class="submit-link" href="/submit">Submit</a>
    </nav>
  </div>
</header>

<style>
  .site-header {
    border-bottom: 1px solid var(--line);
    position: sticky;
    top: 0;
    z-index: 10;
    background: color-mix(in srgb, var(--paper) 88%, white);
    backdrop-filter: blur(14px);
  }

  .header-inner {
    align-items: center;
    display: flex;
    gap: 24px;
    min-height: 66px;
    justify-content: space-between;
    max-width: var(--page-max);
  }

  .brand {
    align-items: center;
    display: inline-flex;
    font-weight: 800;
    gap: 10px;
    white-space: nowrap;
  }

  .brand-mark {
    align-items: center;
    background: linear-gradient(135deg, var(--ink), #394135);
    border-radius: 8px;
    color: var(--paper);
    display: inline-flex;
    font-size: 0.8rem;
    height: 32px;
    justify-content: center;
    width: 32px;
  }

  nav {
    display: flex;
    flex-wrap: wrap;
    gap: 8px 18px;
    justify-content: flex-end;
  }

  nav a {
    color: var(--muted);
    font-size: 0.92rem;
    font-weight: 650;
  }

  nav a:hover {
    color: var(--ink);
  }

  .submit-link {
    border: 1px solid var(--line);
    border-radius: 8px;
    color: var(--ink);
    padding: 8px 12px;
  }

  @media (max-width: 760px) {
    .header-inner {
      min-height: 60px;
    }

    nav {
      gap: 10px;
    }

    nav a:not(.submit-link) {
      display: none;
    }
  }
</style>
