---
import BaseLayout from "@/layouts/BaseLayout.astro";
---
<BaseLayout title="Methodology" description="How OpenAgent.bot defines openness, attributes facts and produces rankings." path="/methodology">
  <div class="page-shell"><div class="page-grid">
    <section class="page-section"><span class="kicker">Methodology / v0.2</span><h1 class="page-title">Transparent data<br />about transparent systems.</h1><p class="lede">Every material fact should answer three questions: who said it, when did we observe it, and what changed since the previous observation?</p></section>
    <section class="page-section methodology-layout"><nav aria-label="Methodology sections"><span class="data-label">On this page</span><a href="#inclusion">Inclusion</a><a href="#openness">Openness</a><a href="#sources">Source priority</a><a href="#changes">Observations</a><a href="#rankings">Rankings</a><a href="#corrections">Corrections</a></nav><article class="prose">
      <h2 id="inclusion">What belongs in the database</h2><p>An entity can be an agent, agent framework, model, robot, robotics framework, hardware platform, simulator, protocol, tool or dataset. Inclusion is broader than endorsement. Closed projects may appear when they are necessary evidence for understanding an open ecosystem, but their openness state remains explicit.</p>
      <h2 id="openness">Openness is multidimensional</h2><p>“Open” is not one boolean. We track code, model weights, data, hardware, documentation and governance separately. The public summary—open source, open weights, open core, source available, closed or unknown—is derived from those evidenced facets.</p>
      <h2 id="sources">Source priority</h2><ol><li><strong>Canonical:</strong> repository APIs, package registries, model cards and release metadata.</li><li><strong>Official:</strong> organization newsrooms, engineering blogs, research pages and documentation.</li><li><strong>Community evidence:</strong> maintainer discussions and reputable technical references.</li><li><strong>Discovery signals:</strong> social posts and aggregators. These can suggest what to inspect, but cannot establish a material fact alone.</li></ol>
      <h2 id="changes">Observation and change</h2><p>Connectors append observations. A current-fact materialization makes the product fast; the historical observation remains immutable. When a normalized value changes, the system emits a change event containing the old value, new value, source and timestamp.</p>
      <h2 id="rankings">Rankings</h2><p>Rankings operate on versioned historical snapshots. Open Momentum combines star velocity, release cadence, commit recency, contributor breadth and openness coverage. Raw project size is deliberately not the score. Every published entry will expose its component values and methodology version.</p>
      <h2 id="corrections">Corrections</h2><p>A correction creates a new observation and preserves the earlier record. We do not silently rewrite historical facts. Source conflicts are resolved by trust tier, recency and field-specific rules; unresolved conflicts remain visible as uncertainty. <a href="/corrections">Submit a correction →</a></p>
    </article></section>
  </div></div>
</BaseLayout>
<style>.methodology-layout{display:grid;gap:64px;grid-template-columns:210px minmax(0,760px)}.methodology-layout>nav{align-self:start;display:grid;position:sticky;top:96px}.methodology-layout>nav a{border-bottom:1px solid var(--line);font-family:var(--mono);font-size:.59rem;padding:11px 0;text-transform:uppercase}.methodology-layout>nav a:hover{padding-left:6px}@media(max-width:760px){.methodology-layout{grid-template-columns:1fr}.methodology-layout>nav{display:none}}</style>
